<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 120 120" width="120" height="120">
  <defs>
    <linearGradient id="frame-edge" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="currentColor" stop-opacity="0.15"/>
      <stop offset="100%" stop-color="currentColor" stop-opacity="0.05"/>
    </linearGradient>
  </defs>
  <!-- Rounded square frame -->
  <rect x="8" y="8" width="104" height="104" rx="24" ry="24"
        fill="none"
        stroke="currentColor" stroke-opacity="0.2" stroke-width="1.5"/>
  <rect x="8" y="8" width="104" height="104" rx="24" ry="24"
        fill="url(#frame-edge)"/>
  <!-- Circle (the dot  one thing) -->
  <circle cx="60" cy="56" r="30"
          fill="currentColor" fill-opacity="0.9"/>
  <!-- Checkmark carved into the circle -->
  <path d="M47 56 L55 64 L73 46"
        stroke="#0A0A0F" stroke-width="4" stroke-linecap="round" stroke-linejoin="round"
        fill="none"/>
  <!-- Subtle highlight on the circle (145° light) -->
  <circle cx="52" cy="48" r="12"
          fill="currentColor" fill-opacity="0.1"/>
  <!-- Logotype below -->
  <text x="60" y="102" text-anchor="middle"
        font-family="Inter, -apple-system, BlinkMacSystemFont, sans-serif"
        font-size="11" font-weight="500" letter-spacing="0.02em"
        fill="currentColor" fill-opacity="0.6">
    single-todo
  </text>
</svg>
